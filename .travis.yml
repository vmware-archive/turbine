language: go

go:
- 1.2
- tip

before_install:
- export PATH=$HOME/gopath/bin:$PATH
- go get github.com/tools/godep

install:
- godep restore
- godep go install github.com/onsi/ginko/ginkgo

script:
- ginkgo -r